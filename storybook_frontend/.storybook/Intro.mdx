import { Meta } from '@storybook/blocks';

<Meta title="Introduction/Read Me" />

# Storybook for Ocean Professional

- Start: `npm start` (binds 0.0.0.0:3000)
- CRA app (optional): `npm run start:app` (port 3300)
- Build static Storybook: `npm run build-storybook`
